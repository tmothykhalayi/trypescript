"use strict";
let character;
let age;
let isloggedin;
age = 30;
isloggedin = true;
// Array of strings
let ninja = [];
// Union types array (string | number | boolean)
let mixed = [];
mixed.push(20);
mixed.push('hello');
mixed.push(true);
console.log(mixed);
// Object Declaration (Explicitly)
let user = {
    name: 'John',
    age: 25,
    isLoggedIn: true
};
console.log(user);
